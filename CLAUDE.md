# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

Internal Telintec back-office REST API — Flask + flask-restx — exposing endpoints under `/GUI/api/v1/<area>` for RRHH (HR), SM (material requests), Almacén (inventory), SGI, Bitácora, Dashboards, Admin/Presales, Admin/Collections, Common, Misc, and Login/UserSystem. The Flask app is consumed by separate desktop GUI clients (the repo's name is historical; this project is the API, not a GUI). MySQL is the backing store; JWT bearer tokens carry user permissions.

## Change docs

Per-change design notes live in [`Docs/`](Docs/) (Spanish, one `.md` per change, relative `../` links into the code). When you make a non-trivial change — new/altered endpoint, cross-layer refactor, schema/contract change — add a short doc there following the existing style ([`heads_crud_fields_sync.md`](Docs/heads_crud_fields_sync.md) is a good template: the 4 layers touched, what changed, and an "Al modificar" section).

**Always include a front contract block** in any doc that adds/changes an endpoint: a "Contrato mínimo para el front" section with the concrete request/response contract so a front agent can integrate without reading the backend. It must state (at minimum) the **auth header** — `Authorization` carries the **raw JWT, NOT `Bearer <token>`** (`jwt.decode` on the raw header, no prefix stripping) — the base path, request shape (fields/types, multipart is one-file-per-request), response content-types (the `{data,msg,error}` JSON envelope; **file downloads return a binary blob on 200 and the JSON envelope on 4xx** — the front branches on status), concrete success/error examples per status code, and any gotchas (truthy query values, etc.). [`remission_combined_pdf.md`](Docs/remission_combined_pdf.md) has a reference block. Los pendientes abiertos de **todas** las áreas (back y front) viven consolidados en [`Docs/pendientes.md`](Docs/pendientes.md) — un listado con link al doc completo de cada ítem; al cerrar o crear un pendiente en cualquier doc, reflejarlo ahí. Current docs:

- [`contract_items_qa_item_id_upsert.md`](Docs/contract_items_qa_item_id_upsert.md) — items de contrato/cotización: la llave del PK `quotation_items.id` se renombra a **`qa_item_id`** en corte duro (request **y** response: `PUT /contract`, `PUT /quotation`, `GET /quotation/<id>`, `GET /contracts/products`; los alias SQL `qi.id AS item_id` consumidos por posición no se tocan). Causa raíz del bug "los items nuevos no se crean": **`wtforms_json` deja un `null` de JSON como `None` sin aplicar el `default` del campo**, y el midleware decidía con `id == 0` → `None == 0` es `False` → el item nuevo caía en `UPDATE ... WHERE id = NULL` → 0 filas con `flag=True` (type_sql 3) → `200` silencioso. Ahora el upsert es el mismo contrato que los items de remisión (`null`/`0`/ausente → crear, `is_erased == 1` → borrar **por igualdad**, antes `!= 0` hacía que `is_erased: null` **borrara** el item), más guard de pertenencia contra la cotización (id ajeno → se crea, no reescribe otra fila) y `AND quotation_id = %s` en el `UPDATE`/`DELETE`. El `rowcount` solo se valida en el `DELETE` (en un `UPDATE` de MySQL son las filas *cambiadas*: guardar sin editar da 0). `quotation_id` ahora sale de la BD (omitirlo creaba una cotización nueva y dejaba los items previos huérfanos), `GET /contracts/products` gana los 5 campos que hacían destructivo el viaje redondo, y de paso: `POST /contract` creaba items con `contract_id NULL` (args desalineados, rompía el enlace SM ↔ partida), 3 `get_quotation` posicionales (500 en `GET /quotation/<id>` y `PUT /quotation`), `contract_id = result[5]` inexistente (nuevo `get_contract_id_by_quotation`), `create_item_quotation` con `type_sql` 3→4. Incluye "Contrato mínimo para el front" con tabla de traducción de llaves. Pendiente: el `PUT` no devuelve los `qa_item_id` creados (el front debe re-`GET`); `comment` sigue siendo campo fantasma.
- [`contract_crud_response_envelope.md`](Docs/contract_crud_response_envelope.md) — `/contract` and `/quotation` CRUD aligned to a fixed `{data, msg, error}` response envelope (Spanish, concise `msg` with ID, `error` always present); fixes set-literal/nested-data/inverted-rollback bugs and removes a misapplied `marshal_with`.
- [`po_folio_generation_from_contract_code.md`](Docs/po_folio_generation_from_contract_code.md) — PO folio generation now reads the contract `code` column (last 4 digits) instead of `metadata.contract_number`; max+1-per-pattern consecutive logic; `get_purchase_orders` (no-items listing).
- [`heads_crud_fields_sync.md`](Docs/heads_crud_fields_sync.md) — `/head` CRUD aligned with its `fetch` (editable `name`, vacant `employee` as `NULL`, `area` wired end-to-end).
- [`purchase_list_pdf.md`](Docs/purchase_list_pdf.md) — purchase list PDF generation.
- [`sm_items_extra_info_url_fix.md`](Docs/sm_items_extra_info_url_fix.md) — SM items `extra_info` URL fix.
- [`payroll_s3_upload.md`](Docs/payroll_s3_upload.md) — payroll S3 upload.
- [`po_sm_deliveries_tracking.md`](Docs/po_sm_deliveries_tracking.md) — al crear/actualizar una OC se rastrean los cambios hacia los `deliveries` de los items de SM vinculados (vía `id_item_sm`): mapea `folio`/`folio_supplier` (sobrescriben) y `time_delivery` (segmento marcado `[Entrega estimada: …]` en `comment`), match por `delivery.id_order`, crea el delivery si no existe; registra en el `history` de la SM; no fatal (la OC sigue 200/201, detalle a `msg`/log/notificación). Helper `sync_sm_deliveries_from_po` + controllers `get_sm_item_deliveries_db`/`update_deliveries_sm_item_db`.
- [`remission_history_changes.md`](Docs/remission_history_changes.md) — historial resumido de cambios de la remisión en el mismo campo `activity_reports.history` (json): corrige el bug del key `timestamp` (era `timestamp: timestamp` → la fecha como clave; ahora `"timestamp": timestamp`, 13 ocurrencias) y agrega un objeto `changes:{metadata:[{field,before,after}], items:[{qa_item_id,description,action:'updated|added|removed',fields:[...]}]}`. Campos curados (no diff genérico); el front mapea `field→etiqueta`; entrada siempre se agrega aunque `changes` quede vacío. Cubre `update_remission_from_api` (metadata+items) y `update_remission_control_table_from_api` (solo metadata). Helpers `_diff_history_fields`/`_diff_remission_items`/`_remission_meta_from_*`.
- [`quotation_remission_unit_price_split.md`](Docs/quotation_remission_unit_price_split.md) — el `unit_price` del ítem (fila compartida cotización/remisión en `quotation_activity_items`) ahora separa sugerido vs real: el sugerido de la cotización se conserva en `extra_info["unit_price_quotation"]` y `unit_price` pasa a ser el real de la remisión. Sugerido siempre escrito desde la cotización; remisión preserva (el form sigue mandando un solo `unit_price`); editar cotización con `report_id` ya presente protege el real (solo actualiza el sugerido); ítem sin cotización → `unit_price_quotation=0`; los GET exponen `unit_price_quotation` plano. Pendiente: historial de remisiones por ítem.
- [`remission_module_fields_and_balance.md`](Docs/remission_module_fields_and_balance.md) — campos por módulo (REMISIONES / Control de Reportes / CONTROL SALDOS) acumulados en `extra_info` de la misma fila de `activity_reports`; endpoint nuevo `PUT /remissionBalance` (solo edita, no crea; no toca columnas base); merge por módulo y por llaves presentes en el JSON crudo (`_extra_info_updates` + `_*_EXTRA_KEY_MAP`); ningún campo nuevo obligatorio en back, estatus como integer; nombres canónicos resueltos (`totalSinIva`→`total_sin_iva`, `statusReport`→`status_report`, `projection_balance`, `ot`+`ticket_number` separados, sin `ot_ticket_number`); un solo GET de remisiones expone todo aplanado; fixes: `KeyError 'items'` en POST control table, `project` guardado como tupla, `quotation_id or > 0` TypeError.
- [`sm_response_envelope.md`](Docs/sm_response_envelope.md) — los 24 endpoints de `/sm` alineados al envelope `{data, msg, error}`; 4 `marshal_with` removidos; bugs corregidos (`/add/urgent` `return data`, `/cancel` tupla invertida, `msg:"ok"` en 400 de `/item`); `msg` español-con-ID y `data` estructurado a `{"id_*": N}` en escrituras (detalle largo solo a log/notificación; fallos parciales a `error` como lista); único pendiente abierto: bug KPI `(critical_date - critical_date)` (a futuro KPIs configurables).
- [`remission_pdf_download.md`](Docs/remission_pdf_download.md) — `GET /remission/download/pdf/<int:id_report>?iva_rate=0.16`, descarga el PDF formal de una remisión (header/logo Telintec, metadata de contrato/pedido, tabla de items, totales); solo la página 1 del documento de referencia (sin anexos de reporte de materiales/fotos). `partida` por item resuelto con `LEFT JOIN` nuevo a `quotation_items` en `get_remission_by_id` (sin cambio de esquema); `No. Contrato Marco` = `contracts.code` vía `get_contract`; SUBTOTAL = `Σ line_total` de items (no `extra_info.total_sin_iva`); `iva_rate` opcional en query, default 0.16. De paso corrige un bug real en `get_contract(data_token, id_contract)` que hacía fallar siempre la búsqueda de un contrato por ID (afectaba también `GET /contract/<id>`). Pendiente: anexar al PDF los `files` (adjuntos S3) de la remisión y el reporte de materiales formato Ternium.
- [`sm_download_not_found_and_parsing.md`](Docs/sm_download_not_found_and_parsing.md) — descarga de SM (PDF/Excel) con `sm_id` inexistente ya no truena con 500: causa raíz `JSON_ARRAYAGG` sin `GROUP BY` en `get_sm_by_id`/`get_sm_by_folio` (una fila de NULLs para ids inexistentes; el fix protege los 8 call sites), not-found → envelope `{data, msg, error}` con 404; parseo defensivo en `dowload_file_sm` (fechas NULL → celda vacía, `comment` NULL → estatus "pendiente", `observations` NULL → `[]`, filtra items con `id` NULL de SMs sin items). De paso: typo `dispached`→`dispatched` (la columna de suministrado imprimía "None" siempre), contador de items congelado en 1, encabezado Excel "Stock"→"C. Suministrado" (igual que el PDF).
- [`sm_pdf_grid_redesign.md`](Docs/sm_pdf_grid_redesign.md) — PDF de SM rediseñado en cuadrícula: metadata (2 pares label|valor por fila) e items con celdas `rect` y labels/encabezados en celeste `#BDD7EE` (texto negro bold); valores largos hacen wrap dentro de la celda (fix del desborde de "Personal Telintec"); multipágina repite header Telintec + encabezados de columna (metadata solo pág. 1); tabla de entregas/firmas para llenar a mano (una fila por attachment de `extra_info["files"]`, mínimo 1, cap 20) + campo "Fecha de Entrega Completa"; corrige encabezados UDM/Cantidad cruzados respecto a los datos del PDF anterior; helpers `_sm_*` nuevos solo para SM (los compartidos con `InventoryStoragePDF`/`ReturnMaterials` intactos); observaciones fuera del documento. El formato de diseño está capturado como skill en `.claude/skills/pdf-design/`. Pendiente: pre-llenar las filas de entrega/firmas con la info de cada attachment (fecha, quién entrega/recibe) cuando `create_sm_attachment_api` la guarde en `extra_info["files"]`.
- [`sm_attachment_delivery_title.md`](Docs/sm_attachment_delivery_title.md) — `POST /sm/attachment-<id_sm>` guarda `timestamp` (fecha de la entrega) y `title` ("Entrega N", del form multipart opcional `title` o autogenerado) en cada objeto de `extra_info["files"]`; parser propio `expected_files_attachment_sm` en `api_sm_models` (el genérico de SGI queda intacto). Fix: el endpoint fallaba **siempre** porque trataba la tupla de `get_sm_by_id` como lista de SMs (`isinstance(result, list)` rechazaba todo). Files viejos sin los campos nuevos siguen válidos (leer con `.get`). Pendiente: el PDF debe pre-llenar la tabla de entregas con estos campos.
- [`po_movements_inbound_match.md`](Docs/po_movements_inbound_match.md) — `GET /admin/collections/purchase/movements/match` (query params opcionales `status`/`date_from`/`date_to`/`folio`): concilia OCs con movimientos de entrada de almacén vía `reference` (extra_info del movimiento, en mayúsculas) vs `folio`/`folio_supplier` de la OC — exacto primero, fallback contains marcado `parcial`, `matched_by` por campo, un movimiento puede aparecer bajo varias OCs; `reception_status` simple (`con_entradas`/`sin_entradas`). SMs relacionadas: primario por `deliveries` (`id_order` == OC, con el objeto delivery), fallback por `extra_info.id_item_sm` de los items de la OC (`link` lo indica); por item: `quantity`, `dispatched`. Match en Python (midleware `match_po_movements_and_sms`); controller nuevo `get_sm_items_deliveries_for_match_db` (una query: items con deliveries + ids fallback). Misma visibilidad que `fetch_purchase_orders` (no admin/gerente → solo sus OCs). El doc incluye sección "Para el front": ejemplo real de respuesta, referencia campo por campo (tipos, cuándo vienen `""`/`null`, `sm_id` del movimiento es string libre — no FK) y casos borde de UI (movimiento duplicado entre OCs, `delivery: null` en fallback). Pendiente: conciliación por cantidades producto a producto.
- [`sm_pdf_delivery_signatures.md`](Docs/sm_pdf_delivery_signatures.md) — el PDF de la SM ahora **incrusta la firma de quien recibe** en la tabla de entregas (una fila por attachment de `extra_info["files"]`): baja la imagen de S3 (`S3_ADMIN_BUCKET`, key = `file["path"]`), la pre-redimensiona con PIL (tope 600px, para no engordar el PDF) y la dibuja en la celda "Firma de Quien Recibe" con `drawImage`/`ImageReader` (aspect ratio preservado, `mask="auto"`); pre-llena "No." y "Fecha de Entrega" = `timestamp` + `title`. Alto de fila dinámico (26pt texto / ~60pt con firma), **paginación por fila** repitiendo encabezados (el cap 20 sigue como tope), "Firma de Quien Entrega" siempre en blanco. No fatal: attachment no dibujable (`pdf`/`zip`), falla de S3 o decode → celda en blanco (a mano) + log. Nuevo contrato de `FileSmPDF`: `delivery_files` (lista) en vez de `delivery_rows` (int); descarga en midleware (`_build_sm_delivery_files`/`_downscale_signature_image`), el form solo recibe rutas locales. Solo PDF (Excel sin cambios). Cierra el pendiente de [`sm_pdf_grid_redesign.md`](Docs/sm_pdf_grid_redesign.md). Pendiente: capturar/incrustar también la firma de quien entrega.
- [`sm_pdf_item_status_colors.md`](Docs/sm_pdf_item_status_colors.md) — semáforo de surtido en la tabla de items del PDF de SM: **verde `#C6EFCE` en toda la fila** cuando `dispatched >= quantity` (el sobre-despacho cuenta como completo), **amarillo `#FFEB9C` solo en la celda `No.`** con surtido parcial, **rojo `#FFC7CE` solo en `No.`** sin nada surtido, y **sin color** si `quantity <= 0` (dato incompleto, evita una SM entera en rojo). Pasteles tipo Excel, misma familia que el celeste de la casa; sin leyenda (la fila ya trae `Cantidad`/`C. Suministrado`/`Estatus` en texto). El color entra por params opcionales en los helpers **genéricos** (`cell_fills` en `print_grid_row`, `fill_color` en `_grid_draw_cell`) cuyo default deja a `InventoryStoragePDF` idéntico; la clasificación vive en `_sm_item_fills` (forma), el midleware no calcula color. Coerción defensiva de `quantity`/`dispatched` (un string del JSON haría `TypeError` y tumbaría el PDF). De paso corrige la columna `Estatus`, que imprimía **"pendiente" en items parcialmente surtidos**: `"(despachado)"` no es substring de `"(semidespachado)"`, así que se agregó esa rama — **después** de la de despachado, porque el `comment` acumula marcadores y un item que fue parcial y luego se completó lleva ambos. Solo PDF (Excel sin color).
- [`remission_combined_pdf.md`](Docs/remission_combined_pdf.md) — PDF **combinado** de una remisión (`GET /remission/download/pdf/<id>?full=1`): fusiona con PyMuPDF/fitz la Remisión (pág. 1 generada, con firmas incrustadas) + anexos (PDFs tal cual, imágenes ajustadas a A4) + hoja(s) de EVIDENCIA FOTOGRÁFICA generada(s) (estilo casa `pdf-design`, rejilla 2×3 vertical, Folio = C-numbers distintos por página). Sin `full` = solo la remisión (histórico intacto). Los anexos se suben con `POST /remission/attachment-<id>` (multipart) que ahora acepta `category` (`photo|anexo|firma|otro`) + `folio` + `title` y los guarda en `activity_reports.files`; `firma` se incrusta en la pág. 1 (recuadro/estatus por nombre `firma-realizado`/`firma-recibido`), `otro` se excluye, archivos viejos sin `category` → heurística por nombre/extensión. **Corrige 3 bugs que rompían siempre la subida** (`data["id_report"]` vs `id_voucher`; tupla de `get_remission_by_id` tratada como lista; args `files`/`status` invertidos en `update_report_activity_files`) + los mismos en la descarga de attachment. No fatal por archivo (falla de S3/anexo → se omite + log). Incluye sección "Para el front". Cierra el pendiente de [`remission_pdf_download.md`](Docs/remission_pdf_download.md).
- [`remission_items_json_remove_fix.md`](Docs/remission_items_json_remove_fix.md) — `GET /admin/collections/remission-<id>` devolvía **N-1 partidas** (con 1 item → `[]`, "sin partidas"). Causa raíz: el `JSON_REMOVE(..., '$[0]')` de `get_remission_by_id` estaba puesto para limpiar la fila fantasma del `LEFT JOIN` (`JSON_ARRAYAGG` **sí incluye el `null`**, a diferencia de casi todos los agregados → `[null]`), pero **se aplicaba siempre**, no solo en ese caso. Reemplazado por `IF(COUNT(qai.qa_item_id) = 0, JSON_ARRAY(), JSON_ARRAYAGG(...))` — decide por conteo, no recorta por posición; el `CASE ... ELSE NULL` sobra (ya no entran nulls). `JOIN`/`GROUP BY`/orden de columnas intactos (los 8 call sites indexan por posición, `items` sigue en 16). Cierra también el `500` al editar la **primera** partida (`old_items_map` no la contenía → `KeyError` en `dict_items[...]`), ahora además defensivo con `.get(...) or {}` para ids ajenos. De paso: `get_remission_by_id` no pasaba `data_token` en la rama por id, así que el permiso `tester` leía de dev en vez de la BD de test. Verificado contra el MySQL de dev (conteos reales + caso N≥2 con tabla derivada). Incluye "Contrato mínimo para el front" (`data` es **lista** aun pidiendo un id; `msg`/`error` en `null`). Pendiente: `get_quotation_activity_by_id` tiene el mismo `[null]` sin proteger → `TypeError`/500 en `update_quotation_activity_from_api` con cotización de actividad sin items.
- [`gestion_de_compras.md`](Docs/gestion_de_compras.md) — CRUD nuevo **Gestión de Compras** (FO-COM-01 R3) en admin/collections, ruta `/purchaseManagement`, tabla `sql_telintec_mod_admin.purchase_management` (DDL a mano en [`scripts_db_handle/gestion_de_compras.sql`](scripts_db_handle/gestion_de_compras.sql), sin sistema de migraciones). Seguimiento **flojo**: casi todo opcional, enlaces FK **anulables** (`supplier_id`→suppliers_amc, `client_id`→customers_amc, `contract_id`→contracts, `po_id`→purchase_orders) con texto de respaldo en `extra_info` (`*_text`, `invoice_number`, `requester_text`=SOLICITANTE, `income_date`, `bank_deposit`; se aplanan en los GET). Tres enteros con catálogo (`classification` 0..6, `status` 0..4, `debt_type` 0..2) expuestos en `GET /purchaseManagement/catalogs`; columnas de análisis (`profit`, `profit_percentage`, `cost_ternium_iva`) se guardan tal cual (sin cálculo). Endpoints: `GET` lista (filtros `status`/`classification`/`client_id`/`date_from`/`date_to`/`is_active`/`all`), `GET /<id_pm>`, `POST`, `PUT` (**parcial**: solo escribe llaves presentes en `ns.payload`; `null` vacía), `PUT /cancel` (suave, `is_active=0`, recuperable) y `DELETE` (físico). `approval_date` se auto-sella si `approved=1` sin fecha. 4 capas nuevas (controller `purchases/purchase_management_controller.py`, midleware `MD_PurchaseManagement.py`, modelos `api_purchase_management_models.py`, rutas en `rs_Admin_collections.py`) + `history` JSON. Incluye "Contrato mínimo para el front".
- [`remission_attachment_delete.md`](Docs/remission_attachment_delete.md) — `DELETE /admin/collections/remission/attachment-<id_report>` (body `{filename, reason?, force?}`, permisos `administracion`/`operaciones`): quita el anexo de `activity_reports.files` y **borra el objeto de S3 best-effort** (BD primero; si S3 falla → `200` con `s3_deleted:false` + motivo en `error`, nunca queda un `path` muerto). **Las firmas no se borran** (`category:"firma"` guardada o nombre con `firma-realizado`/`firma-recibido` → `400` aun con `force`), justo para que `activity_reports.status` (que el alta mueve a 1/2) no quede huérfano; `force` solo vence el falso positivo de la heurística en entradas **heredadas** (sin `category` guardada): el alta persiste la categoría efectiva, así que un `firmado_cliente.pdf` subido hoy sin `category` explícita queda protegido para siempre — el front debe mandar `category` en el `POST`. El estatus del reporte nunca se toca y no limita el borrado; archivo inexistente → `400` (igual que el download), reporte inexistente → `404`; `data.files` trae la lista ya actualizada para que el front repinte sin re-`GET`. Dos cambios al `POST`: la llave S3 pasa a `reportActivity/<fecha>/<id_report>/<filename>` (antes dos remisiones de la misma fecha con el mismo nombre de archivo **compartían objeto y se pisaban al subir**; el `DELETE` solo borra de S3 llaves con el id, las heredadas solo se desvinculan) y re-subir el mismo nombre **reemplaza** la entrada en vez de duplicarla (único camino para corregir una firma). Helper nuevo `_load_remission_for_files` compartido por alta/descarga/borrado (mata la tercera copia de los índices `[14]`/`[15]`/`[17]`). Incluye "Contrato mínimo para el front". Pendiente: no hay borrado en lote.
- [`encuestas_refactor.md`](Docs/encuestas_refactor.md) — refactor de encuestas: **motor de evaluación config-driven** (`quizz_eval_engine.py`) que reemplaza el scoring/interpretación hardcodeado por tipo con una **rúbrica en datos** (`files/rubrics/<tipo>.json`, bandas explícitas). Corrige el bug de fondo (la clasificación *puntaje→nivel→interpretación* nunca se aplicaba, así que las recomendaciones siempre caían al default, incluso en Norma 035). Salida uniforme (`total`/`breakdown` recursivo, mismo shape todos los tipos) = contrato del front de resultados. `generate_pdf_from_json` evalúa con el motor y guarda `data_raw["evaluation"]`; shim `_legacy_shape_from_evaluation` mantiene el PDF actual sin tocar reportlab. Migrados: Norma 035 (1,2), eva360 (4) y clima (3); salida(0) cualitativa pendiente. Fixes de paso: bug `id=0` (pisaba fila 0), temp PDF compartido (concurrencia), `print`→`write_log_file`. Incluye "Contrato mínimo para el front".
- [`contract_items_file_import_sections.md`](Docs/contract_items_file_import_sections.md) — import de partidas desde Excel (`POST /admin/presales/contract/items/file`) con **parser tolerante multi-plantilla** (partidas de contrato + remisión; alias de columnas + auto-detección de la fila de encabezados, antes `header=20` hardcodeado rompía CERRAMIENTOS cuyo header está en la fila 18), **errores detallados** (archivo ilegible / plantilla no reconocida / 0 partidas, con detalle al cliente y al log; adiós al falso `200` vacío) y **secciones** (Fase 1+2). Un Excel con varias secciones (p.ej. dos plantas `CERRAMIENTO AM2` / `LARGOS NORTE`; título en `POS.` o `DESCRIPCIÓN`) se carga como grupos con `section_index`; la `POS.` reinicia por sección → `partida` no es única, la llave real es `(contract_id, section_index, partida)`. Persistencia: **columnas nuevas** en `quotation_items` (`section_index INT`, `extra_info JSON` con `{section_title, section_type}`), DDL a mano en [`scripts_db_handle/quotation_items_sections.sql`](scripts_db_handle/quotation_items_sections.sql). `get_quotation` y `get_contracts_with_items` exponen las 3 llaves **al final** del `JSON_OBJECT` (append-only: las 12 primeras son load-bearing para `compare_file_quotation`); `section_type` siempre `"general"` (el humano reclasifica planta/reajuste). **Fase 3 (hecha)**: el enlace SM ↔ ítem (`check_for_partidas_updates`/`update_quotation_item_partida_from_sm` en `MD_SM.py`) matchea por `(contract_id, section_index, partida)`; los ítems de SM (`ItemsFormSMPost`/`PUT`) cargan `section_index` (default 0, sin backfill: lo existente queda en sección 0). Incluye "Contrato mínimo para el front".
- [`eva360_evaluation.md`](Docs/eva360_evaluation.md) — Eva 360 como **proceso multi-evaluador** sobre `tasks_gui`: 1 task de **control** (RH; `eva360_kind:"control"`, `expected_roles`, `status_eval`) + N tasks de **evaluador** (2-4, roles `self|superior|peer|subordinate`, uno por rol), ligadas por `metadata.evaluation_id` (UUID del back). Endpoints nuevos en `rrhh`: `POST /eva360` (crea todo atómico + notifica), `GET /eva360/<id>` (detalle/avance), `GET /eva360/<id>/result` (agregado: general 0-100 promediando **solo respondidas** — respondida = las 9 competencias contestadas —, `by_perspective`, y por competencia `by_role`+`average`+`distribution` de frecuencias 1..5), `PUT /eva360/<id>/complete` (idempotente, solo marca). El llenado usa el CRUD existente (`PUT /misc/task/quizz`, shape per-question `{"4":{"answer":idx}}`, índice 0 = opción "5|"). Motor: `flatten_per_question`, `scale {max,to}` → `total.scaled`, `evaluate_eva360`; rúbrica `files/rubrics/4.json` (9 ítems 4-12, `values:[5,4,3,2,1]`, 45→100, sin bandas — puramente numérico, sin niveles). **Claves nuevas declaradas en `MetadataTasksForm`/`metadata_task_model`** (sin eso el `PUT` de tasks borraba el linking de metadata al validar). Fix template `quizz_eva_360.json`: Q12 duplicada eliminada y 13→12 renumerada (`Eva360.py` itera `range(len)` sin guard); corte duro, no había tasks eva360 en vuelo. Controller nuevo `get_tasks_by_eva360_group` (`body->>'$.metadata.evaluation_id'`); midleware nuevo `MD_Eva360.py`. Incluye "Contrato mínimo para el front". Pendiente: salida(0) cualitativa, PDF del agregado (clima(3) ya migrado — ver `clima_laboral_rubrica.md`).
- [`control_vehiculos_cda.md`](Docs/control_vehiculos_cda.md) — **namespace nuevo `GUI/api/v1/cda`** (Control De Activos; primer activo: vehículos, FO-CDA-02 R3). 6 tablas en `sql_telintec_mod_admin` (DDL en [`scripts_db_handle/control_vehiculos.sql`](scripts_db_handle/control_vehiculos.sql)): `vehicles` maestro (`code` UNIQUE, `accessories` JSON 12 llaves, `is_active` suave) + hijas `vehicle_policies` (renovación = fila nueva; `payments` JSON con slots por `payment_form` 12/4/1), `vehicle_services` (mantenimiento+reparación+servicio en una, `service_type`; km mayor **auto-actualiza** el odómetro), `vehicle_tires` (UNIQUE vehicle+position, 5 posiciones, upsert + `history`), `vehicle_fines`, `vehicle_purchases` (`po_id` flojo) — todas `ON DELETE CASCADE`. **Todo campo calculado se computa en los GET, nunca se almacena**: próximo mantenimiento (último km + 5k/10k según `oil_type`, fecha +6 meses; constantes en `MD_CDA.py`), requiere/días restantes/KPIs, refrendo `AL_DIA|VENCIDO|SIN_REGISTRO` derivado de `refrendo_last_paid`, llanta `expired`, `expected_date`/`overdue` por pago, TOTAL/semana de compras. 16 rutas: CRUD por entidad (PUT **parcial**, cancel suave solo vehículos, DELETE físico) + 6 vistas `GET /view/*` que replican las hojas del Excel + `/catalogs` (incluye `rules`). Permisos `administracion`/`sgi`. 4 capas nuevas (controller `vehicles/vehicles_controller.py`, `MD_CDA.py`, `api_cda_models.py`, `rs_CDA.py` + registro en app.py) + `log_file_cda`. Verificado ciclo completo contra BD dev. Incluye "Contrato mínimo para el front". Pendiente: pantallas del front, import inicial del Excel, recordatorios de vencimientos.
- [`quizz_models_crud.md`](Docs/quizz_models_crud.md) — **CRUD de modelos de encuesta** (`/rrhh/quizz/models`): el par template (cuestionario) + rúbrica (config del motor) migra de archivos a la BD, en el **esquema nuevo `sql_telintec_mod_rrhh`** (primer paso de esquema-por-departamento; tabla `quizz_models`, DDL [`scripts_db_handle/quizz_models.sql`](scripts_db_handle/quizz_models.sql) + seed idempotente `seed_quizz_models.py` — el tipo 0 se inserta auto y se reasigna por el gotcha `NO_AUTO_VALUE_ON_ZERO`). Ciclo de vida `status` 0 borrador (todo editable, no contestable) → 1 activa (**template bloqueado** — `create_task` descarta el snapshot, así que captura y evaluación mapean contra el template vivo; la rúbrica y el name siguen editables: la evaluación es on-read y re-interpretar el historial al corregir una banda es deliberado) → 2 archivada (oculta, historial evaluable, reactivable); transiciones `0→1`/`1→2`/`2→1` vía `PUT /status`; `protected=1` en Norma 035 (jamás borrable); DELETE físico solo sin tasks del tipo. Validación al guardar: template dura (catálogo de widgets), rúbrica dura (`validate_rubric` estructural + `dry_run_rubric` con respuestas sintéticas, nuevas en el motor), consistencia template↔rúbrica solo `warnings`. Corte duro de read paths: `load_rubric`/`evaluate_task` ganan `data_token` y leen BD, `GET /misc/download/quizz/<type>` sirve de BD (mismo shape), `create_task_from_api` exige modelo ACTIVO (antes tipo desconocido → `KeyError` 500); retirados `quizzes_RRHH`/`quizzes_dir_path`/`rubrics_dir_path` y la muerta `extract_data_encuesta`. PDF **genérico de resumen** ([`QuizzGenericReport.py`](templates/forms/QuizzGenericReport.py), skill `pdf-design`) como fallback de `POST /download/quizz/report` para tipos sin generador dedicado — renderiza el shape uniforme, scored y cualitativo. 4 capas nuevas (controller `rrhh/quizz_models_controller.py`, `MD_QuizzModels.py`, `api_quizz_models_models.py` — `template`/`rubric` van por `ns.payload` crudo, WTForms no modela dicts —, 6 rutas en `rs_RRHH.py`). Alcance: modelos nuevos = respondente único (el flujo multi-evaluador queda exclusivo de eva360). Verificado ciclo completo contra BD dev (26 checks) + PDFs renderizados. Incluye "Contrato mínimo para el front" con catálogo de widgets. Pendientes: pantalla front de administración, PDFs dedicados por encuesta, editor de rúbricas, correr DDL+seed en test/prod.
- [`clima_laboral_rubrica.md`](Docs/clima_laboral_rubrica.md) — clima laboral (tipo 3) migrado al motor config-driven: cuestionario nuevo de RH (45 preguntas, 1 sección, Likert 5) + rúbrica `files/rubrics/3.json` con **% de percepción positiva**: `% = positivas/(positivas+negativas)×100`, **Neutral se anula** (`values` con `null` = respuesta excluida, capacidad nueva del motor), 9 categorías con **mapeo cíclico mod 9** (5 ítems c/u), **pregunta 34 invertida** (map aparte), total = promedio de categorías (`total_agg:"avg_breakdown"`, nuevo), bandas contiguas <80 necesita_mejora / 80–<85 adecuado / 85–90 bueno / >90 excelente (borde con `high:90.01`). Categoría 100% neutral → `score:null`, sin nivel, fuera del total. Motor además: `agg:"avg"` vacío → `None` (sum conserva 0), adapter de `subquestions` sin rango `items` (contador corrido 1-based) y `evaluate_group` (conteo agrupado multi-respondente). Endpoint nuevo `GET /rrhh/quizzes/summary/<int:type_q>` (`date_from`/`date_to` inclusivos sobre el timestamp del task) = tabla organizacional (respondents + % por categoría + total; `detail` = % positivo por pregunta). Corte duro (cuestionario viejo de 12 secciones incompatible; en dev solo 3 tasks vacías). **Fix crítico de paso**: el template commiteado era JSON inválido (un `50` colgante) → `GET /misc/download/quizz/3` devolvía 400 siempre. Incluye "Contrato mínimo para el front". Pendiente: PDF individual (shim legacy) y export de la tabla organizacional.
- [`migracion_esquemas_rrhh.md`](Docs/migracion_esquemas_rrhh.md) — **plan por fases** para mover el dominio RRHH de `sql_telintec` al esquema por departamento `sql_telintec_mod_rrhh`. **Fase 1 (lote S2, junto con la consolidación de namespaces `misc`+`rrhh`)**: `tasks_gui` → **`quizz_tasks`** (100% de sus filas son quizzes en dev y prod; simétrica con `quizz_models`; regla nueva: las tasks futuras se dividen **por departamento asignador**) vía **vista puente** — `RENAME TABLE` + `CREATE VIEW sql_telintec.tasks_gui` actualizable `SQL SECURITY INVOKER` — porque `is_tester` hace que la BD de test la compartan **dos versiones del código a la vez** (el DDL corre en las 3 BDs desacoplado de los deploys; script en [`scripts_db_handle/migracion_quizz_tasks.sql`](scripts_db_handle/migracion_quizz_tasks.sql); solo 7 literales en 2 archivos). **Fase 1 ejecutada 2026-08-07** (DDL en las 3 BDs + código + smoke verde; falta solo el `DROP` de las vistas cuando prod corra el código nuevo — todo DDL lo revisa/ejecuta el usuario). **Fase 2 (adelantada y ejecutada 2026-08-07, un solo lote)**: las 5 RRHH-privadas (`examenes_med`/`fichajes`/`bitacora_rh`/`vacations`/`payroll`) + catálogo `aptitude` (0 refs en código) movidas con [`scripts_db_handle/migracion_fase2_rrhh.sql`](scripts_db_handle/migracion_fase2_rrhh.sql) (**nombres idénticos**; DDL en las 3 BDs verificado, las FK hacia `employees` sobrevivieron al `RENAME` y quedaron cross-schema) + barrido de 89 reemplazos en 6 controllers, grep-cero, smoke verde vs dev; mismo tren de deploy que S2 — falta hoja del tester (secciones afectadas), deploy de prod y el `DROP` único de las 7 vistas puente. `employees`/`departments`/`users_system` = **núcleo compartido**, se quedan en `sql_telintec` (un `mod_core` sería otra iniciativa). Sin cambio de contrato para el front (cero cambios de API).
- [`consolidacion_namespaces_encuestas.md`](Docs/consolidacion_namespaces_encuestas.md) — los 3 recursos de encuestas de `misc` movidos a `rrhh` en **corte duro** (viejas → 404): `POST/PUT/DELETE /rrhh/task/quizz`, `GET /rrhh/task/<emp_id>` (auto-acceso), `GET /rrhh/download/quizz/<type_q>`; misma lógica/forms/auth, solo cambia el prefijo — con esto **todo** encuestas vive bajo `rrhh` y `misc` queda solo con lo no-encuesta. `get_task_by_id_employee` mudada de `Functions_midleware_misc` a `Functions_midleware_RRHH`. Ejecutado en el mismo lote S2 que la Fase 1 de la migración de esquema (los 7 literales `tasks_gui`→`quizz_tasks` viajaron aquí). Verificado ciclo completo POST→GET→PUT→DELETE vs dev + viejas 404 + `misc` restante vivo. Incluye "Contrato mínimo para el front" (tabla de traducción de rutas + gotcha del `PUT`: omitir llaves de fecha vacías del `metadata` al re-mandarlo, el validador rechaza `""`/`null`).
- [`rrhh_download_csv_unpack_fix.md`](Docs/rrhh_download_csv_unpack_fix.md) — los 3 GET de descarga CSV de RRHH (`/download/employees/<status>`, `/medical`, `/vacations`) tronaban con 500 (`ValueError` de unpack, destapados en la comprobación S1 del tercero): los SELECT de los controllers **compartidos** ganaron columnas para consumidores JSON (`department_id`+`usernames`, `extra_info`, `renovacion`) y los sitios CSV quedaron con el conteo viejo. Fix mínimo: unpacks alineados (CSV **byte-idéntico**, columnas nuevas descartadas; los SELECT no se recortan — son load-bearing en los otros call sites) + endurecimiento de la misma clase: guards `(x or "").replace` en columnas nullable y la tupla de error de `create_csv_file_employees` (antes `send_file(str(tupla))` → 500) ahora envelope `{data,msg,error}` 400. Verificado contra BD dev (anchos 22/9/6 + CSV end-to-end). Incluye "Contrato mínimo para el front". Pendientes: apellido omitido en employees (`l_name` no se escribe), CSV sin quoting (newline en un valor parte la fila — caso real en dev), rutas relativas hardcodeadas.

## Run / lint

- **Run dev server**: `python wsgi.py` — binds 127.0.0.1:5000 with `debug=True`. `app.py` is the Flask app factory; `wsgi.py` is the entry point.
- **Type check**: `pyrefly check` — config in [pyrefly.toml](pyrefly.toml). Uses `.venv/Scripts/python.exe`. Note: many DB call sites carry `# pyrefly: ignore` because `execute_sql`'s return type is union-typed (see below).
- **Tests**: there is no test framework wired up. `Tests/`, `tester*.py`, `*test.py`, and `*_test.json` are all gitignored — treat any file matching those patterns as scratch.
- **Dependencies**: `pip install -r requirements.txt`. Heavy ML/data deps (tensorflow, keras, sklearn, matplotlib, pandas) are present because some endpoints generate reports/PDFs and run model inference; don't drop them when trimming.

## Environment & secrets

[static/constants.py](static/constants.py) hard-codes `environment = "dev"` at the top and that flag drives:
- which `.env` to load (`.env` in dev vs `../.env` in prod)
- which secret keys to read for DB host/user/pass (`HOST_DB` vs `HOST_DB_AWS`, etc.)
- where `domain.pem` is found

When promoting/testing prod behavior, flip that constant — there is no `FLASK_ENV` or env-var override.

`.env` is at repo root (gitignored). Required keys include `HOST_DB`/`USER_SQL`/`PASS_SQL` (dev), `HOST_DB_AWS`/... (prod), `HOST_DB_TEST`/... (test), and `TOKEN_MASTER_KEY` (JWT signing key).

## Architecture — the layers that matter

The codebase has a strict 4-layer pattern. Stay inside it; cross-layer shortcuts will surprise reviewers.

```
HTTP                templates/resources/rs_<area>.py        flask-restx Namespaces, route definitions
  ↓
Orchestration       templates/resources/midleware/*.py      business logic, multi-controller composition
  ↓                 templates/resources/methods/*.py        login/auth helpers, area-specific aux
  ↓
DB                  templates/controllers/<domain>/*.py     raw SQL via execute_sql
  ↓
Driver              templates/database/connection.py        mysql.connector wrapper

Validation/swagger  static/Models/api_*_models.py           flask-restx api.model + WTForms Forms
```

Each `rs_<area>.py` defines one `Namespace("GUI/api/v1/<area>")` and is registered in [app.py](app.py). To add a new area: create the namespace module, add `from ... import ns as ns_<area>` and `api.add_namespace(ns_<area>)` in app.py.

The `controllers/` subtree is organized by domain (`employees/`, `product/`, `purchases/`, `material_request/`, `tickets/`, `vouchers/`, `chatbot/`, `notifications/`, etc.), not by HTTP route — a single namespace can pull from many controllers via its midleware.

## The `execute_sql` convention (important)

Every DB call goes through [templates/database/connection.py](templates/database/connection.py). It returns `(flag: bool, error: str, result)` and the shape of `result` depends on the integer `type_sql`:

- `1` → `fetchone()` (single tuple or `[]`)
- `2` → `fetchall()` (list of tuples)
- `3` → `rowcount` (int, after commit) — for UPDATE/DELETE
- `4` → `lastrowid` (int, after commit) — for INSERT
- `5` → `fetchall()` without param substitution (raw query)

There is also `execute_sql_multiple(sql, values_list, type_sql, data_token)` that iterates `values_list` column-major (transposes inside the function) — read it before calling, the indexing is unusual.

Both accept an optional `data_token`; if `data_token["is_tester"]` is true, they redirect to the test DB host (`HOST_DB_TEST` etc.). This is how the "tester" permission swaps databases at runtime.

**`error` is already a `str`** (the second tuple element). When you put it in a response envelope's `error` field, write `"error": error` — **not** `str(error)`, which pyrefly flags as `Unnecessary str() call`. Reserve `str(...)` for things that genuinely aren't strings: `Exception` objects caught in `except` blocks (`str(e)`), and the union-typed `result` (whose shape varies by `type_sql`). Same rule for f-strings — `f"...{error}"`, not `f"...{str(error)}"`.

## Auth pattern — every endpoint starts the same way

```python
flag, data_token, msg = token_verification_procedure(request, department="rrhh")
if not flag:
    return {"error": msg if msg != "" else "No autorizado. Token invalido"}, 401
validator = SomeForm.from_json(ns.payload)   # WTForms validation
if not validator.validate():
    return {"errors": validator.errors}, 400
data = validator.data
data_out, code = some_midleware_fn(data, data_token)
return data_out, code
```

[templates/resources/methods/Functions_Aux_Login.py](templates/resources/methods/Functions_Aux_Login.py) implements this. Two things to know:

1. **`department` can be a string or a list** — list = OR-match across departments.
2. **Permission match is substring + case-insensitive** (`verify_department_permission`), AND any permission containing `"administrator"` always passes. Adding a new permission name with the substring `"administrator"` would silently grant admin everywhere — be careful when editing [static/permissions_models.json](static/permissions_models.json).

Endpoints conventionally return `(dict, http_code)`. Midleware functions follow the same shape so `rs_*` can pass results through unchanged.

## Models / forms duality

Each `static/Models/api_<area>_models.py` defines two parallel things for each endpoint:
- a `flask_restx` `api.model(...)` — used in `@ns.expect(...)` for swagger docs and the `expected_headers_per` model adds the `Authorization` header expectation.
- a WTForms `Form` subclass (e.g. `EmployeeInsertForm`) — used at runtime via `Form.from_json(ns.payload)` (enabled by `wtforms_json.init`). This is the actual validator; the api.model is doc-only.

When adding a field, update **both**. The `# pyrefly: ignore` on `from_json` calls is intentional — wtforms-json patches the class dynamically and pyrefly can't see it.

## Daemons and side processes

[templates/daemons/](templates/daemons/) holds background workers (medical notification sweeps, file/peripheral watchers). They are NOT started by `app.py` — they're invoked via separate scripts or threads spawned from specific endpoints. `files/flags_daemons.json` is the on-disk flag store coordinating their state. If you're touching anything daemon-related, grep for `update_flag_daemons` to find the lifecycle calls.

## File-based state under `files/`

Several caches, logs, and config live as files (paths centralized in [static/constants.py](static/constants.py)):
- `files/logs/<area>/...` — per-area log files (bitacora, sm, sgi_chv, sgi_vouchers, db, admin, almacen, po, rh, users)
- `files/*_cache.pkl` and `files/*_cache.json` — endpoint-level caches; gitignored
- `files/contracts/`, `files/files_fichaje/`, `files/quizz_out/` — generated artifacts
- `files/Pmodels/` — pickled / Keras models (for ML-backed endpoints)
- `files/settings.json`, `files/flags_daemons.json` — runtime config

Don't read or write under `files/` from new code without using the constants — paths are referenced from many places.

## Naming / locale notes

- Naming mixes English and Spanish (`midleware/` is intentional spelling, `bitacora`, `almacen`, `fichaje`, `nomina`, `RRHH`). Don't "fix" these — they are referenced by string in dozens of places (permission checks, log paths, namespace URLs).
- Module headers carry `__author__` / `__date__` strings on most files; preserve that style for new files in this repo.

## Common pitfalls

- **Don't bypass `execute_sql`** with raw `mysql.connector` calls — you'll lose the test-DB switching and the consistent `(flag, error, result)` shape.
- **Don't add `try/except` around `from_json` validation** — the form layer already returns 400 with field-level errors.
- **The `secrets` dict** loaded from `.env` is read once at import time in `static/constants.py`; restart the app after `.env` changes.
- **Adding a new namespace** requires both the import and `api.add_namespace(...)` in [app.py](app.py); flask-restx silently ignores routes from un-registered namespaces.
